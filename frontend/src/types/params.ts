export type Params = {
  templateSizePx: number;
  gainMultiplier: number;
  smoothing: number;
  dwellEnabled: boolean;
  dwellTimeMs: number;
  autoStart: boolean;
  startPause: string;
  recenter: string;
};
